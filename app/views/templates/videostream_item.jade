.videostreamitem(title=content.title + " - " + content.subTitle + "\n" + action)
  .audiovisualwork
    .placeholder
      h3= content.title
      .subtitle= content.subTitle

  .date= moment(timestamp).format('LT L')
